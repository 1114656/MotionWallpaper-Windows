# 安全策略

## 支持范围

MotionWallpaper 目前是预发布软件。安全修复只应用到 `main` 分支的最新提交；旧提交和自行修改的构建不在支持范围内。

## 报告安全漏洞

请勿为疑似安全漏洞创建公开 Issue。请使用 GitHub 仓库的“私密漏洞报告”功能，并提供受影响的提交、复现步骤、预期影响和可能的修复建议。

报告中不要包含私人媒体、访问令牌、个人路径或其他敏感数据，优先使用最小化的合成样例。

维护者应在收到信息完整的报告后七天内确认。漏洞复现并完成修复后，再协调公开披露时间。

## 重点安全区域

以下区域的报告尤其有价值：

- 媒体导入、元数据解析、缩略图和 FFmpeg 调用；
- 路径穿越、符号链接/重解析点和回收站操作；
- UI、Agent 与 Renderer 之间的命令和确认通道；
- 配置损坏和跨进程写入竞争；
- 开机启动、进程归属和孤儿进程清理。
