# MotionWallpaper 优化路线

本文记录后续性能与功耗优化方向。优化必须遵守三个前提：不降低视频清晰度、正常播放保持流畅、状态切换不闪出 Windows 静态壁纸。

## 已完成的播放内核改造（2026-08-22）

- 使用 Media Foundation 帧服务器取得视频帧，不再让系统播放器窗口直接承担桌面呈现。
- 使用 D3D11 双缓冲交换链和 DirectComposition 常驻视觉层。暂停、循环衔接、`Alt + Tab` 和桌面/屏保切换期间，最后一帧始终由 GPU 前缓冲保留，不再用缩略图或 Windows 静态壁纸填空。
- 冻结命令等待下一帧真正提交并与垂直同步对齐后才暂停解码；恢复命令等待首个新帧提交后才确认播放。旧画面会一直保留到新画面准备好。
- 播放器使用高精度单次等待定时器，在预计下一帧到达前约四分之一帧周期醒来，范围限制为 3～24 ms；帧尚未就绪时只做 2 ms 短重试。60 FPS 通常为 13 ms 探测，120 FPS 为 6 ms，既减少空转又不等待完整帧周期。播放链路不使用 EcoQoS。
- Agent 通过系统会话、显示器电源、显示拓扑、Explorer、前台窗口及窗口尺寸变化事件驱动状态变化。每次计算都会枚举当前桌面全部可见、未最小化且未被 DWM 隐藏的顶层窗口，不再只检查聚焦窗口。F11、播放器全屏按钮、最大化/还原和 `Alt + Tab` 都会即时重新计算覆盖状态，重复事件会合并，500 ms 检查仅作为兜底。
- 静态图片通过 WIC 解码后只上传一次完整分辨率 D3D11 表面，呈现完成后释放双缓冲，不运行帧定时器或视频解码器。
- agent 与 renderer 使用单写者状态机、单调递增修订号和 ACK。快速连续点击只执行最终状态，过期命令不能反向覆盖新状态。
- 播放开关设置保留 350 ms 防抖，避免一次快速点击触发多次配置写入和播放器重建。
- GPU 选择先读取视频原生分辨率与帧率。低于 4K60 等效像素吞吐量时使用主显示器显卡以降低功耗；达到或超过阈值时优先高性能显卡，防止核显视频引擎饱和后掉帧。素材不转码、不缩放、不降帧；软件模式仍使用 WARP。
- 冻结后停止呈现定时器和视频解码工作，但普通暂停保留已暂停的 Media Foundation 解码器，避免恢复时重新从关键帧高速追帧。仅当 Windows 报告低内存压力时，才把完整分辨率最后一帧迁移到单张 DirectComposition 表面并释放解码器与双缓冲交换链。

播放期间的双缓冲仍会占用必要的显存和驱动私有提交。冻结压缩后只保留一张完整分辨率表面，这是“完全不露出 Windows 静态壁纸”的最低固定成本。私有提交不能等同于正在活跃使用的物理内存。

## 当前策略

- 用户活动且关闭“用户活动时播放”时，冻结当前呈现帧并暂停解码。
- 达到闲置时间后恢复视频，作为动态屏保播放。
- 任意可见全屏窗口覆盖且禁止继续播放时等待一张完整帧提交后暂停；普通暂停保持解码器热驻留且不再解码。低内存深度释放后返回桌面时，单帧表面继续遮挡预热过程，首个新帧到达后再接管画面。
- 解码模式默认优先使用系统支持的硬件解码路径。
- 循环播放使用 Media Foundation 原生循环；首帧出现后，缓冲和循环衔接期间保留最后呈现帧。
- 屏保窗口切回 WorkerW 桌面层时先完成窗口挂载，再冻结视频表面，避免暂停表面因重挂载失效而露出系统壁纸。
- Agent 与 Renderer 在创建窗口前启用 Per-Monitor DPI V2，显示尺寸统一使用物理像素。主屏模式跟随当前主显示器；每屏独立模式按稳定显示器标识恢复媒体绑定，并为每块活动显示器建立等比居中裁切的全尺寸输出，兼容不同分辨率、不同缩放率和负坐标布局。分辨率、DPI 或显示拓扑变化后自动重建 Renderer。

## 第一阶段：低风险功耗优化

1. 确认硬件解码路径
   - 记录实际使用的解码器以及是否进入 GPU Video Decode 引擎。
   - `auto` 模式优先硬件解码，不支持时才回退软件解码。
   - 不转码、不缩放源文件，保持原始分辨率与画质。

2. 降低后台调度开销
   - 仅常驻策略 Agent 使用 EcoQoS；渲染器正常播放时保持正常调度优先级。
   - 将可替换的定时轮询改为系统事件通知。
   - 播放时不使用高频空转循环，暂停时不产生周期性唤醒。

3. 减少无效工作
   - 视频完全被全屏窗口覆盖时及时暂停。
   - 锁屏、显示器关闭和系统睡眠期间释放解码资源。
   - 电池模式可选择更积极的暂停和释放延迟。

全屏暂停期间先保留准确帧并停止调度与解码。普通状态保持解码器热驻留；只有系统低内存时才迁移到单帧表面并释放解码器。深度释放后的恢复仍由旧表面遮挡解码预热过程，不会露出缩略图或 Windows 静态壁纸。

## 第二阶段：冻结后的深度休眠（已完成）

目标是在用户活动且画面长时间冻结时，让 CPU、GPU 和磁盘读取接近零，同时保留活动发生瞬间的画面。

当前流程：

1. 下一张完整帧提交时同步捕获准确的当前帧，然后暂停 Media Foundation。
2. 普通暂停保留已经停止工作的解码器，并每 30 秒低频检查一次系统低内存通知；此状态没有帧调度、视频解码或磁盘读取。
3. 只有系统处于低内存压力时，DWM 才确认单帧 DirectComposition 表面已接管，随后释放 Media Engine、双缓冲、解码缓冲和视频文件句柄。
4. 恢复时在单帧表面后创建交换链、预热解码器并定位到记录时间。
5. 新视频首帧呈现并由 DWM 提交后一次性交接，避免黑屏、缩略图或 Windows 壁纸闪烁。

普通冻结不再调用 `SetProcessWorkingSetSize` 强制清空工作集。只在紧凑状态释放 GPU 对象后调用 `IDXGIDevice3::Trim`，避免快速恢复时因重新分页造成卡顿。

隔离测试已验证恢复时旧帧保持到新帧到达，渲染器能从保存的媒体时间继续播放，且不会退出或露出底层壁纸。

## 第三阶段：播放期间优化

- 已改为时间戳驱动的高精度单次调度：在预计帧到达前醒来，未就绪时 2 ms 短重试；同时去掉每帧重建 RenderTargetView 与会被视频全屏覆盖的无效清屏。
- 已加入按原生像素吞吐量选择适配器的策略：普通素材保留在低功耗显示适配器，4K60 及以上负载优先高性能适配器。2026-08-24 使用 3840×2160、约 239.76 FPS 素材实测，AMD 610M 视频引擎持续接近 100%；切换 RTX 5070 后，视频解码约 47%、两块 GPU 的 3D 合计约 22%，暂停与恢复 ACK 均正常。
- 多显示器使用相同媒体时按“媒体 + 显卡适配器”合并 Renderer：同一显卡只解码一次，但每块显示器拥有严格等于自身物理尺寸的独立 swapchain、裁切和冻结表面，不再创建虚拟桌面大小的巨型纹理。跨显卡或不同媒体使用隔离的解码链路。
- 对 HEVC/H.265、H.264 和不同 MOV/MP4 封装分别验证硬件解码。
- 保持静音和无音频渲染，避免创建不必要的音频设备链路。
- 自动平衡和低功耗会在源分辨率或帧率高于当前显示目标时准备优化缓存；源文件存在时，原画优先始终使用源文件。

## 超高帧率素材的性能档位

- `原画优先`：源文件存在时直接播放导入源文件，不创建或选择性能缓存；源文件已被用户删除时不可用。
- `自动平衡（推荐）`：目标帧率为 `min(源帧率, 120, 目标显示器最高刷新率)`，目标分辨率覆盖使用同一媒体的最高必要显示分辨率。
- `低功耗`：目标帧率为 `min(源帧率, 60)`，分辨率匹配当前显示需求。
- 两个优化档位都不放大低分辨率源文件，也不为低帧率源插帧。
- 插电与电池供电时保持用户选择的档位；电池只能暂停后台生成任务，不得把自动平衡暗中变成 60 FPS。
- 性能缓存根据当前目标显示器的物理像素生成“等比覆盖”尺寸：不低于屏幕实际需要的可见细节，不再解码最终不会显示的多余像素。当屏幕分辨率大于等于源视频时不放大。
- 性能缓存固定放在媒体目录的 `Variants` 子目录。用户可以单独删除源文件并保留封面、名称、元数据和已有副本；删除后原画模式与重新生成副本不可用。
- 性能缓存总量限制为 8 GiB；生成前按最坏输出体积预检剩余空间并保留 256 MiB 安全余量，清理时跳过本次会话仍在使用的缓存。
- 同一媒体分配到多个显示器时继续复用一个 Renderer 和一条解码链，不会为每块屏幕重复生成缓存。
- 目标缓存未完成时继续使用源文件或兼容的已有副本；生成完成后由新 Renderer 首帧 ACK 再接管旧画面。失败、硬件不支持、输出尺寸/帧率错误或文件异常膨胀时自动删除临时文件并回退到仍然存在的可播放文件。
- 具体档位、多显示器、电池和独立管理页规则见 `docs/WALLPAPER_PERFORMANCE_AND_VARIANTS.md`。
- 准备进程在启动前就进入低于正常的调度优先级、EcoQoS 和低内存优先级，并在完成后退出。FFmpeg 及动态库不常驻，因此不会增加正常播放和冻结状态的进程内存。

## 2026-08-24 稳态实测

测试素材为 3840×2160、约 239.76 FPS、HEVC Main10，主屏 2560×1440，RTX 5070 Laptop GPU。

- 旧的 3840×2160/120 FPS 中间缓存：Video Decode 平均 19.14%，3D 平均 8.77%，Renderer 工作集约 137 MB。
- 新的 2560×1440/60 FPS 低功耗缓存：Video Decode 平均 4.94%，3D 平均 2.66%，Renderer 工作集 83～98 MB（平均 91 MB），CPU 约占 24 逻辑核整机的 0.20%。
- 上述是按 Renderer PID 过滤的 GPU Engine 计数器，不包含其他程序，也不把一次性 FFmpeg 缓存生成计入稳态。

## 测试与验收

每次性能优化都应在同一个测试视频、同一显示器设置下记录：

- 播放 60 秒的平均 CPU、GPU 3D、GPU Video Decode、磁盘读取和专用 GPU 内存。
- 冻结 60 秒的 CPU 时间增量、GPU 利用率、磁盘读取和工作集。
- 连续循环至少 30 次，不能出现黑帧、缩略图或 Windows 静态壁纸。
- 用户活动、进入屏保、退出屏保、全屏覆盖与解除覆盖各测试 20 次。
- 快速和连续执行 `Alt + Tab` 20 次，渲染窗口不得重复挂载或闪出底层壁纸。
- 每个设置开关快速连续点击 20 次，只应用最终状态，播放器不得反复停止和重建。
- 休眠、唤醒、锁定和解锁后画面及进程状态正确。

只有在画质和衔接测试通过后，优化项才进入默认配置。
